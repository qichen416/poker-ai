# Poker AI 新增内容说明

> 更新日期：2026-06-24  
> 本文档用于集中记录本轮新增和完善的功能，原始项目说明仍保留在 `README.md`。

## 一、C++ 扑克底层

### 1. 单挑规则环境

完善了 `SelfPlayEnv`，目前支持：

- 大小盲初始化：小盲 50、大盲 100。
- 双方初始筹码 20000。
- 随机发放双方底牌和五张公共牌。
- 翻前、翻牌、转牌、河牌和摊牌阶段推进。
- `fold`、`check`、`call`、`bet`、`raise`、`allin`。
- 本街投入、最小加注和待跟注金额计算。
- 弃牌和摊牌后的底池结算。
- 平局分池和总筹码守恒。
- 终局奖励：英雄终局筹码减去初始筹码。
- 自动执行对手策略。

新增接口：

```python
env.legal_actions()
env.is_action_legal(action, amount=0)
```

其中 C++ 环境中的 `bet/raise amount` 表示玩家在本街“加到”的总投入。

### 2. 卡牌数据校验

- 花色必须处于 `0~3`。
- 点数必须处于 `0~12`。
- 非法卡牌会在进入牌型评估和模拟前抛出异常。

### 3. 胜率计算

`WinRateCalculator` 新增：

- 已知牌重复检查。
- 公共牌数量检查。
- 对手数量和模拟次数检查。
- 剩余牌不足检查。
- 批量输入长度检查。
- 平局底池权益计算。

返回值现在表示底池权益，而不只是“没有输”的概率。例如两人必然平局时返回 `0.5`。

## 二、Python/C++ 绑定

pybind11 新增暴露：

- `ActionRecord` 构造函数。
- `SelfPlayEnv.legal_actions()`。
- `SelfPlayEnv.is_action_legal()`。

同时修复 Windows 多配置构建把新 `_core.pyd` 输出到子目录、导致 Python 加载旧扩展的问题。

## 三、协议与状态同步

重写了 `engine/parser.py`，现在能够同步：

- 英雄位置和手牌。
- 大小盲与初始筹码。
- 双方剩余筹码。
- 当前底池。
- 英雄的 `to_call`。
- 双方每条街的累计投入。
- 完整动作历史 `ActionRecord`。
- flop、turn、river 公共牌。

协议中的：

```text
bet N
raise N
```

统一表示“本次动作额外投入 N 筹码”。

进入新的街道时，本街投入和 `to_call` 会自动清零；之前的投入已经保存在底池中。

## 四、TCP 通信

重写了 `engine/client.py`，新增：

- 按换行符进行协议分帧。
- TCP 半包拼接。
- TCP 粘包拆分。
- 接收缓冲区。
- 64 KiB 单帧保护。
- 连接失败重试。
- 对端断开后的自动重连。
- 主动关闭后禁止自动重连。

动作发送失败后不会自动重新发送，因为服务端可能已经收到第一次动作；自动重放可能造成重复下注。

## 五、程序主循环

从 `engine/main.py` 提取：

```python
run_game(client, protocol_parser, decide)
```

该函数负责：

1. 接收一条完整平台消息。
2. 更新 `GameState`。
3. 调用决策函数。
4. 发送动作。
5. 发送成功后记录英雄动作。

只有 `sendall()` 成功后才更新本地筹码，防止本地状态领先于服务端。

## 六、Mock Server

完善了 `tests/mock_server.py`：

- 支持后台线程启动。
- 支持主动停止。
- 支持使用随机空闲端口。
- 使用固定随机种子生成可重复牌局。
- 能记录并校验客户端动作。
- 使用接收缓冲区处理 TCP 字节流。
- 模拟包含 `raise`、`bet`、`call` 和 `check` 的完整协议流程。

## 七、新增测试

新增或完善以下测试：

- 非法卡牌测试。
- 牌型评估测试。
- 平局权益测试。
- 胜率输入校验测试。
- 单挑环境初始化测试。
- 合法动作测试。
- 下注、跟注、弃牌和全押测试。
- 完整摊牌测试。
- 协议筹码和动作历史同步测试。
- 跨街状态重置测试。
- TCP 半包与粘包测试。
- 真实 socket 断线重连测试。
- Mock Server 端到端协议测试。

## 八、本地验证结果

```text
C++ 测试：5/5 通过
Python 测试：14/14 通过
1000 局完整底层模拟通过
Mock Server 端到端对局通过
```

1000 局模拟中验证了：

- 每局均能正常到达终局。
- 终局底池被清空。
- 双方总筹码始终为 40000。
- 无崩溃和状态死循环。

## 九、尚未完成

以下内容不属于本轮新增，仍待后续实现：

- 决策层合法动作掩码。
- CFR 信息集抽象。
- MCCFR 训练。
- CFR 策略保存和加载。
- DRL 特征编码。
- DRL 真实网络推理。
- PPO 训练循环。
- 对手模型与决策融合。
- 比赛平台 `gameover` 结果和最终筹码结算协议。

## 十、建议下一步

优先实现合法决策保护层：

1. 根据比赛 `GameState` 生成合法动作。
2. 对 CFR/DRL 概率应用动作掩码。
3. 校验下注和加注金额。
4. 提供合法超时兜底动作。
5. 使用 Mock Server 验证不会发送非法动作。
