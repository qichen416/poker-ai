# 德州扑克 AI 项目规划 v2.0

## Context

- **比赛**：全国大学生计算机博弈大赛 · 两人单挑德州扑克
- **目标**：全国前 5-10 名
- **时间**：2026/06/05 起，8 周（6 月轻量 + 7 月冲刺）
- **算力**：5 台 4060 笔记本 | 5 人团队
- **当前**：Phase 1 早期（Week 1），代码为骨架/原型状态——**完全正常**

v1.0 的技术路径方向正确。v2.0 保留核心公式，加入 5 个优化项，目标是让 AI 从"不犯错"升级为"能杀人"。

---

## 一、核心公式（保留 + 扩展）

```
π_final = α·π_DRL + (1-α)·π_CFR + β·π_exploit + γ·π_search
```

| 参数           | v1.0        | v2.0                                                           |
| -------------- | ----------- | -------------------------------------------------------------- |
| α（DRL权重）  | 固定 0.5    | **动态**：翻后早期 0.3（CFR 主导），河牌 0.7（DRL 主导） |
| β（剥削强度） | 0（未接入） | **0.05~0.15**，随对手样本量增加而增大                    |
| γ（搜索权重） | 0（无）     | **0.1~0.3**，仅 Turn/River 大底池启用                    |

### 四层决策架构

```
Layer 1: CFR 基线（盾）—— GTO 保证，永远不会犯大错
Layer 2: DRL 泛化（脑）—— 翻后局面编码→动作，覆盖 CFR 抽象不到的牌面
Layer 3: 对手剥削（矛）—— 对手类型分类 + 预训练偏移向量，针对性杀人
Layer 4: 实时搜索（眼）—— 关键节点深度限制 CFR 求解，现场算最优策略
```

---

## 二、5 个新增优化项

### 优化 1：手牌抽象 + 信息集编码

**当前问题**：`make_info_set_key` 返回 `"default"`——所有局面共用一个策略。

**方案**：

- 翻前：直接编码 169 类起手牌（13 对子 + 78 同花 + 78 非同花）
- 翻后：手牌按牌力分 ~1000 桶 + 公共牌按纹理分 ~200 类
- 目标信息集数：翻前 ~1300，翻后 ~5×10⁶（可存入内存）

### 优化 2：CFR → DRL 知识蒸馏

**当前问题**：DRL 从零自博弈（PPO），4060 训不出来。

**方案**：

```
Step 1: CFR 在抽象化游戏上训练（CPU，2-3 天）→ 产生"教师"策略表
Step 2: 行为克隆——DRL 模仿 CFR 策略（4060，几小时）→ 初始化网络权重
Step 3: PPO 微调——自博弈突破 CFR 抽象局限（4060，1-2 天）
Step 4: 推理时纯网络前向传播，<1ms
```

### 优化 3：子博弈实时求解

**方案**：

- 仅 Turn/River 的"大底池 + 深筹码"局面启用（约 10% 决策点）
- 从当前局面展开博弈树（深度 3-5 层），树内 CFR 迭代 50-500 次
- 叶子节点用 DRL value_head 估值，省去遍历到底
- 时间预算 1-3 秒

**效果**：对手无法通过历史对局预判你的策略——每次都是现场算的。

### 优化 4：下注尺度精炼

**当前问题**：只有 `bet pot` 一个尺寸。

**方案**：预定义 5 档 → DRL 选择最佳的：

```
1/3 pot —— 薄价值、引诱跟注
1/2 pot —— 标准化下注
3/4 pot —— 施压保护
1.5x pot —— 超注极化
all-in —— 终极武器
```

### 优化 5：对手建模升级

**当前问题**：3 个统计量 + 手写 if-else 规则。

**方案**：

- 对手 5 分类：Rock(紧弱) / LAG(松凶) / TAG(紧凶) / Station(跟注站) / Maniac(疯子)
- 每类预训练最优剥削偏移向量（离线用 CFR 对抗该类型计算）
- 比赛中 ≥20 手后自动匹配类型，偏移 ≤15%

---

## 三、8 周时间线

### Phase 1：准备期（6 月，晚上/周末）

```
Week 1-2（6/01-6/14）: 核心模块补齐
├── hand_evaluator.cpp — 完成 9 种牌型判定
├── cfr_engine.cpp — 信息集编码 + MCCFR 遍历 + 序列化
├── drl/infer.py — 真正调用网络 + 状态编码器
├── drl/train.py — 行为克隆 + PPO 训练循环
└── 里程碑：C++ 单元测试全绿

Week 3-4（6/15-6/28）: 原型验证
├── 翻前 CFR 策略表生成（169 起手牌，CPU 2 天）
├── DRL 行为克隆训练
├── Mock 服务器完整打通一局
├── opponent_model 接入 fusion.py
├── α 动态化
└── 里程碑 M1：Mock 对局 100 手无崩溃，接口冻结
```

### Phase 2：冲刺期（7 月，全天）

```
Week 5（6/29-7/05）: 全集群运行
├── CFR 翻后抽象策略表（3 台并行，~5×10⁶ 信息集）
├── DRL PPO 微调（4060 × 2 天）
├── 子博弈搜索 C++ 实现
├── 5 档下注尺度接入
└── 里程碑 M2：CFR 基线可用，DRL 能推理

Week 6（7/06-7/12）: 混合联调
├── 四组件融合（CFR + DRL + 剥削 + 搜索）
├── 对手类型预训练
├── 1000 局自博弈评估
└── 里程碑 M3：混合策略首次联调成功

Week 7（7/13-7/19）: 大规模评估
├── Elo 评估 vs 不同对手
├── 参数扫描调优
├── 5000 局连续压测
└── 目标：Elo 进入前 20%

Week 8（7/20-7/26）: 冻结提交
├── main 分支锁死
├── 比赛平台最终对接
├── 文档提交 + 纯 CFR fallback 备好
└── 里程碑 M4：比赛版本冻结
```

---

## 四、团队分工

| 角色                      | Phase 1 任务                             | Phase 2 任务                   |
| ------------------------- | ---------------------------------------- | ------------------------------ |
| **队长 + DRL**      | 网络改造、状态编码、行为克隆             | PPO 微调、融合层联调、参数精调 |
| **C++ + CFR 核心**  | hand_evaluator 牌型判定、CFR 遍历+序列化 | 子博弈搜索实现、翻后策略表生成 |
| **CFR 工程 + 翻前** | 手牌抽象设计、翻前 GTO 表生成            | 翻后抽象方案、策略压缩         |
| **通信 + 逻辑**     | Socket 客户端、协议解析修复、Mock 测试   | 比赛平台对接、超时保护验证     |
| **数据 + 评估**     | 对手建模升级、Elo 系统                   | 大规模测试、对手类型预训练     |

---

## 五、关键文件

### Phase 1 必改（P0）

| 文件                             | 改动                             | 优先级 |
| -------------------------------- | -------------------------------- | ------ |
| `cpp/src/hand_evaluator.cpp`   | 补齐 9 种牌型判定逻辑（计数法）  | P0     |
| `cpp/src/cfr_engine.cpp`       | 信息集编码 + MCCFR 遍历 + 序列化 | P0     |
| `cpp/src/environment.cpp`      | 完整游戏状态机                   | P0     |
| `cpp/tests/test_hand_eval.cpp` | 9 种牌型全覆盖测试               | P0     |
| `cpp/tests/test_cfr.cpp`       | CFR 收敛性测试                   | P0     |
| `drl/state_encoder.py`         | 新建：GameState → 张量编码      | P0     |
| `drl/infer.py`                 | 改为真正调用神经网络             | P0     |
| `drl/train.py`                 | 行为克隆 + PPO 训练循环          | P0     |
| `engine/fusion.py`             | 接入 opponent_model + α 动态化  | P1     |
| `engine/parser.py`             | 取消 ActionRecord 注释           | P1     |

### Phase 2 改/新增

| 文件                                   | 改动                                 |
| -------------------------------------- | ------------------------------------ |
| `cpp/include/poker/subgame_solver.h` | 新建                                 |
| `cpp/src/subgame_solver.cpp`         | 新建：深度限制子博弈求解             |
| `cpp/python_bindings/bindings.cpp`   | 追加 SubgameSolver 绑定              |
| `drl/network.py`                     | 增加 value_head 输出用于叶子节点估值 |
| `engine/fusion.py`                   | 四组件融合 + 5 档下注尺度            |
| `eval/opponent_model.py`             | 对手 5 分类升级                      |

---

## 六、实施顺序（依赖关系）

```
第1步: HandEvaluator 牌型判定 — 无依赖，所有模块的基础
第2步: CFR 信息集编码 — 可并行于第1步
第3步: CFR MCCFR 遍历 — 依赖 1+2
第4步: SelfPlayEnv 状态机 — 依赖 1
第5步: DRL 状态编码 + 推理 — 可并行于 2-4
第6步: DRL 行为克隆训练 — 依赖 3+5
第7步: 对手模型接入 fusion — 可任意时间
第8步: 翻前策略表生成 — 依赖 3
第9步: 子博弈搜索 — 依赖 1+3+4（Phase 2）
第10步: PPO 微调 — 依赖 6+9（Phase 2）
```

---

## 七、风险与 Fallback

| 风险               | 概率 | 应对                                           |
| ------------------ | ---- | ---------------------------------------------- |
| DRL 训练不收敛     | 中   | Fallback: 纯 CFR + 对手建模，目标前 30%        |
| 子博弈搜索超时     | 低   | 仅 Turn/River 大底池启用，预算 3 秒，兜底 call |
| CFR 翻后信息集过大 | 低   | 更粗粒度抽象，降到 10⁶ 级                     |
| 某成员 6 月缺席    | 高   | 允许缺席，Week 4 末必须补课理解接口            |

---

## 八、验证方案

| 阶段   | 验证方式                   | 通过标准             |
| ------ | -------------------------- | -------------------- |
| Week 2 | `ctest -C Release`       | C++ 测试全绿         |
| Week 4 | Mock 对局                  | 100 手无崩溃         |
| Week 6 | 自博弈 1000 局 vs CFR 基线 | 胜率 >55%            |
| Week 7 | 5000 局压测                | 零崩溃，P99 延迟 <5s |
| Week 8 | 5 种对手各 200 局          | 每种胜率 >55%        |
