# Poker AI 项目规划

> 生成日期: 2026-06-18 | 版本: v1.0

---

## 一、项目概述

### 1.1 目标

构建一个**德州扑克（No-Limit Texas Hold'em）AI**，能接入比赛平台进行对局。AI 采用 **CFR（反事实遗憾最小化）+ DRL（深度强化学习）+ 对手建模** 的混合架构。

### 1.2 技术栈

| 层 | 技术 | 用途 |
|---|---|---|
| 计算核心 | C++17 + AVX2 | 手牌评估 / CFR 遍历 / 胜率计算 / 规则引擎 |
| 胶水层 | pybind11 | C++ → Python 绑定 |
| 深度学习 | Python 3.10 + PyTorch 2.12 + CUDA 12.6 | PPO 训练 / 推理 |
| 通信 | TCP Socket | 与比赛平台交互 |
| 配置 | YAML | 超参数管理 |
| 构建 | CMake 3.20+ / setuptools | C++ 编译 + Python 包管理 |

### 1.3 已具备的基础设施

```
Card 建模 ✅ ──── HandEvaluator (7选5) ✅ ──── WinRateCalculator (蒙特卡洛) ✅
GameState ✅ ──── pybind11 绑定 ✅ ──── TCP 通信 ✅ ──── 协议解析 ✅
对手建模 ✅ ──── Elo 评分 ✅ ──── Mock 服务器 ✅ ──── VSCode 配置 ✅
```

---

## 二、总架构

```
┌────────────────────────────────────────────────────────────────┐
│                     比赛平台 (TCP Socket)                        │
└──────┬─────────────────────────────────────────────────┬───────┘
       │ recv                                           │ send
       ▼                                                │
┌──────────────────────────────────────────────────────────────────┐
│  engine/main.py — 主循环 (连接→收消息→决策→发动作)                │
│  ├── engine/client.py       TCP 收发 + 超时保护                    │
│  ├── engine/parser.py       协议文本 → GameState 结构               │
│  └── engine/fusion.py       决策融合层 (CFR + DRL + 对手偏移)       │
└──────────────────────────────────────────────────────────────────┘
       │                    │                    │
       ▼                    ▼                    ▼
┌──────────────┐  ┌─────────────────┐  ┌──────────────────┐
│ CFR Engine   │  │ DRL Inference   │  │ Opponent Model   │
│ (C++ 核心)   │  │ (Python/PyTorch) │  │ (Python 统计)    │
│ 翻前 GTO 表  │  │ 翻后实时推理    │  │ VPIP/PFR/AF 偏移  │
│ 翻后基线策略 │  │ PPO 网络前向   │  │ 剥削策略调整     │
└──────────────┘  └─────────────────┘  └──────────────────┘
       │                    │
       ▼                    ▼
┌──────────────────────────────────────────────────────────────────┐
│  C++ 底层库 (poker_core._core.pyd)                                │
│  ├── HandEvaluator    7 选 5 最优牌型判定                          │
│  ├── WinRateCalculator 蒙特卡洛随机模拟                              │
│  ├── SelfPlayEnv      德州规则引擎 (发牌/下注/比牌)                   │
│  └── CFREngine        MCCFR 遍历 + 策略存储                         │
└──────────────────────────────────────────────────────────────────┘
```

---

## 三、分阶段实施计划

### 🔴 阶段一：CFR 引擎 (MCCFR 算法) — 预计 2~3 周

**这是整个项目的核心基石，翻前策略表和翻后基线策略都依赖它。**

#### 3.1.1 信息集抽象 (Info Set Abstraction)

**目标**: 将巨大的游戏状态空间压缩为有限的信息集键（info set key）。

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 1a. 实现 `make_info_set_key()` | `cpp/src/cfr_engine.cpp` | 根据 `GameState` 生成唯一的字符串键。格式: `"{手牌桶ID}|{公共牌桶ID}|{阶段}|{动作历史摘要}"` |
| 1b. 手牌桶 (Hand Bucketing) | 同上 | 将 169 种起手牌（13×13 同花+非同花）按强度分桶。初始方案: 对子/同花连牌/高牌 共 10 桶 (config: `hand_buckets: 10`) |
| 1c. 公共牌桶 (Board Bucketing) | 同上 | 按公共牌纹理分桶。初始方案: 干燥/湿润/成对 共 5 桶 (config: `board_buckets: 5`) |
| 1d. 动作历史摘要 | 同上 | 将本阶段动作序列压缩为短串。例如 `"rcf"` = raise→call→fold；用序列采样限制长度 |

**依赖条件**: `GameState` (✅ 已完成) — 需要访问 `my_cards`, `community_cards`, `stage`, `history`

**验收标准**:
- 相同语义的局面产生相同的 info set key
- 不同强度的手牌产生不同的 key
- key 的数量在可控范围内（非指数爆炸）

---

#### 3.1.2 MCCFR 遍历算法

**目标**: 实现基于抽样的反事实遗憾最小化 (Monte Carlo CFR)，对游戏树进行遍历并更新每个信息集的遗憾值。

| 子任务 | 详细内容 |
|---|---|
| 2a. 游戏树节点定义 | 定义 C++ 内部结构 `CFRNode`，包含: 当前 GameState / 当前玩家 / 到达概率 p0, p1 |
| 2b. `mccfr_traverse()` 递归函数 | **核心算法**。输入: `(GameState, player, p0, p1)`；逻辑: (1) 若是终局节点 → 返回 payoff (2) 若是机会节点 → 随机发牌并递归 (3) 若是决策节点 → 用 regret matching 计算策略，遍历合法动作，递归收集 counterfactual values，更新 regret_sum_ 和 strategy_sum_ |
| 2c. Chance Sampling 实现 | 在每个机会节点（发公共牌），从剩余牌堆中随机抽取一张（而非遍历所有可能），减少分支因子 |
| 2d. 合法动作集生成 | 根据当前 GameState（底池/筹码/阶段）生成合法动作列表。德州扑克合法动作: `{fold, check, call, bet, raise, allin}` 的子集 |
| 2e. `train_iteration()` 实现 | 调用 `mccfr_traverse()` 完成一次完整遍历，更新所有访问到的信息集 |

**依赖条件**:
- 信息集抽象 (3.1.1) ← **必须先完成**
- `HandEvaluator` (✅ 已完成) — 终局比牌需要
- `WinRateCalculator` (✅ 已完成) — 可选的 roll-out 近似

**验收标准**:
- 单次 `train_iteration()` 能正常完成不崩溃
- 遍历后 `regret_sum_` 和 `strategy_sum_` 非空
- 策略概率和 ≈ 1.0
- 随训练迭代，策略从 uniform 收敛（regret matching 数学保证）

---

#### 3.1.3 策略持久化

**目标**: 将训练好的策略表保存到磁盘，并在推理时加载。

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 3a. `save_strategy()` | `cpp/src/cfr_engine.cpp` | 将 `strategy_sum_`（平均策略）序列化为二进制或 JSON 文件。字段: `info_set_key → [6个动作概率]` |
| 3b. `load_strategy()` | 同上 | 从文件反序列化，填充 `strategy_sum_` |
| 3c. 增量 checkpoint | 同上 | 每 N 次迭代自动保存 (config: `checkpoint_every: 10000`) |
| 3d. Python 端加载接口 | `engine/fusion.py` | `DecisionEngine.__init__()` 中调用 `cfr_engine.load_strategy()` |

**验收标准**:
- `save → load` 往返后策略完全一致
- 文件大小合理（10^5 信息集 × 6 个 double ≈ 几 MB）
- 断点续训可用

---

#### 3.1.4 翻前 GTO 策略表生成

**目标**: 离线运行 MCCFR 生成 169 种起手牌 × 位置的翻前策略表。

| 子任务 | 详细内容 |
|---|---|
| 4a. 翻前训练脚本 | Python 脚本 `scripts/train_preflop.py`，创建单阶段 GameState，运行 100K 次迭代 |
| 4b. 输出格式 | 生成 `data/preflop/gto_table_v1.pkl` — 一个 Python dict: `{(hand_bucket, position) → [fold, check, call, bet, raise, allin 概率]}` |
| 4c. 正确性验证 | 与公开 GTO 表（如 PokerSnowie / Simple Preflop Holdem）对比，一致性 > 85% |

**依赖条件**: MCCFR 遍历 (3.1.2) + 策略持久化 (3.1.3)

---

### 🔴 阶段二：SelfPlayEnv 完善 — 预计 1~2 周

**目标**: 补全德州扑克完整规则引擎，使 AI 能自博弈，生成 DRL 训练数据。

#### 3.2.1 完整游戏流程

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 5a. 庄家/盲注轮转 | `cpp/src/environment.cpp` | 实现 SB/BB/Button 位置轮转。初始: BB 先行动，翻牌后 BUTTON 先行动 |
| 5b. 完整发牌流程 | 同上 | `deal_cards()` → 发 2 张手牌 + 5 张公共牌（flop 3张 → turn 1张 → river 1张），每阶段间插入下注轮 |
| 5c. 阶段推进逻辑 | 同上 | 自动推进: PREFLOP → FLOP → TURN → RIVER → SHOWDOWN |
| 5d. 下注轮逻辑 | 同上 | 每位玩家按顺序行动。下注轮结束条件: 所有存活玩家都行动过 且 没有人面对未跟注的加注 |
| 5e. 边池 (Side Pot) 计算 | 同上 | 当有人 all-in 且筹码不等时，正确计算主池和边池 |
| 5f. `step()` 完整实现 | 同上 | 接收动作 → 更新状态 → 推进游戏 → 返回 (新状态, 奖励, 是否结束) |

**依赖条件**: `GameState` (✅ 已完成)

**验收标准**:
- 完整的一局牌能从头跑到尾不崩溃
- 下注/加注/跟注/all-in 逻辑正确
- 池底分配正确（包括边池情况）
- 单局 1000 次对局统计无异常

---

#### 3.2.2 对手策略集成

| 子任务 | 详细内容 |
|---|---|
| 6a. `run_opponent_turn()` 实现 | 调用 `opponent_policy_` 回调获取动作，执行并更新状态 |
| 6b. 内置 baseline 对手 | 提供 3 种内置对手: (1) 随机 (2) 紧凶 (TAG) (3) 松凶 (LAG) |
| 6c. 历史对手回放 | 从 `data/opponent_db/` 加载真实对手数据，用统计分布采样动作 |

---

### 🔴 阶段三：DRL 训练 + 推理 — 预计 3~4 周

**目标**: 用 PPO 算法训练深度网络进行翻后决策，替代纯 CFR 在复杂翻后局面中的不足。

#### 3.3.1 特征工程 + 环境封装

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 7a. 牌面特征编码 | `drl/environment.py` | 将手牌 + 公共牌编码为 `(4, 13, 2)` 的张量: 4 个 suit 通道 × 13 个 rank 列 × 2 张手牌。持有牌=1，已见牌标记 |
| 7b. 动作历史编码 | 同上 | 将最近 10 条动作记录编码为 `(10, 10)` 张量: `(stage_oh(4) + action_oh(6)` |
| 7c. 标量特征 | 同上 | 底池大小(归一化)、筹码量(归一化)、位置、阶段、对手 VPIP/PFR/AF |
| 7d. `PokerEnv` (Gym 风格) | 同上 | `reset() → obs` / `step(action) → (obs, reward, done, info)` |
| 7e. 奖励函数设计 | 同上 | (1) 终局: ±筹码变化 (2) 非终局: 0 (3) 可选: 小奖励引导激进打法 |

**依赖条件**: SelfPlayEnv (阶段二) ← **必须先完成**

**验收标准**:
- `PokerEnv` 能被 `stable-baselines3` 直接使用
- reset/step 延迟 < 1ms（C++ 端）
- 观测形状: `card=(batch,4,13,2)`, `action=(batch,10,10)`, `scalar=(batch,4)`

---

#### 3.3.2 PPO 训练循环

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 8a. Rollout 收集 | `drl/train.py` | 16 个并行环境 `step()` → 收集 n_steps=512 步轨迹 |
| 8b. GAE 优势计算 | 同上 | Generalized Advantage Estimation (γ=0.99, λ=0.95) |
| 8c. PPO Clip Loss | 同上 | `L = -min(ratio * A, clip(ratio, 1-ε, 1+ε) * A) + c1*VF_loss - c2*entropy` |
| 8d. 训练循环 | 同上 | for epoch in 4: mini-batch 训练 → 裁剪梯度 → 更新参数 |
| 8e. Checkpoint + TensorBoard | 同上 | 每 2 小时保存模型，TensorBoard 记录 loss / policy entropy / 平均回报 |
| 8f. 自博弈对手轮换 | 同上 | 定期将当前最优模型作为对手，避免过拟合固定策略 |

**依赖条件**: PokerEnv (3.3.1) ← **必须先完成**

**验证方案**:
- 训练 loss 下降曲线正常（不震荡不爆炸）
- 每 N 步评估胜率 vs baseline 持续上升
- Elo 评分 vs 旧版本持续增长

---

#### 3.3.3 DRL 推理引擎

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 9a. 特征提取函数 | `drl/infer.py` | 将 `GameState` 转换为网络输入张量（复用 7a-7c 的特征工程代码） |
| 9b. `get_action_probs()` 真实实现 | 同上 | 替换硬编码值: `state → 特征 → 网络前向 → softmax → {动作: 概率}` |
| 9c. 批推理优化 | 同上 | `@torch.no_grad()` + FP16 推理 + batch 支持 |
| 9d. C++ 集成 (可选优化) | 新增 | 将 PyTorch 模型导出为 TorchScript / ONNX，在 C++ 端用 libtorch 推理，消除 Python↔C++ 往返开销 |

**验收标准**:
- 推理延迟 < 2ms（GPU）/ < 10ms（CPU）
- 输出概率和为 1.0
- 加载真实权重后输出 ≠ 硬编码值

---

### 🟡 阶段四：决策融合层完善 — 预计 1 周

**目标**: 将 CFR + DRL + 对手建模三者输出融合为最终决策。

#### 3.4.1 融合策略升级

| 子任务 | 文件 | 详细内容 |
|---|---|---|
| 10a. 动态 α 权重 | `engine/fusion.py` | 翻前 α=0（纯 CFR）；翻后根据公共牌复杂度动态调整：干燥面 α=0.3 / 湿润面 α=0.7 |
| 10b. 对手剥削偏移 | 同上 | 调用 `OpponentModel.get_exploit_offset()` → 偏移动作概率 → 重新归一化 |
| 10c. 下注尺度模型 | 同上 | 将下注额离散化: `{1/3 pot, 1/2 pot, 2/3 pot, pot, 2x pot, all-in}`；扩展动作空间为 `actions × sizes` |
| 10d. 安全性检查 | 同上 | (1) 不能 fold 当 check 可用 (2) 不能 bet < 最小下注 (3) all-in 不能超过持有筹码 |

**依赖条件**: CFR (阶段一) + DRL (阶段三) + Opponent Model (✅ 已完成) — **必须全部完成**

**验收标准**:
- 翻前决策只依赖 CFR
- 翻后决策 ≈ α*CFR + (1-α)*DRL + 对手偏移
- 从不产生非法动作
- 决策总延迟 < DEADLINE_TIMEOUT (5s)

---

### ✅ 阶段五：集成测试与评估 — 预计 2 周

#### 3.5.1 端到端测试

| 子任务 | 详细内容 |
|---|---|
| 11a. Mock 服务器对局 | 用 `tests/mock_server.py` 进行 1000 局完整对局，统计胜率/平均收益 |
| 11b. 错误注入测试 | 模拟网络断连/超时/非法消息，验证 AI 能正确 fallback |
| 11c. 内存泄漏检测 | 长时间运行（10000 局），检查内存/显存是否稳定 |
| 11d. 性能基准测试 | (1) CFR 策略查询 < 0.1ms (2) DRL 推理 < 2ms (3) 总决策 < 100ms |

#### 3.5.2 版本对战 (Elo)

| 子任务 | 详细内容 |
|---|---|
| 12a. 基线对比 | `CFR-only` vs `CFR+DRL` vs `Random` — 各 1000 局 |
| 12b. 消融实验 | 去掉对手建模 / 去掉 DRL / 去掉翻后 CFR — 量化每个模块贡献 |
| 12c. Elo 曲线追踪 | 每次训练 checkpoint 自动跑 Elo 评估，绘制 Elo 增长曲线 |

---

## 四、依赖关系图

```
阶段一: CFR 引擎
  ├── 1.1 信息集抽象 ─────────────┐
  ├── 1.2 MCCFR 遍历 (依赖 1.1) ──┤
  ├── 1.3 策略持久化 (依赖 1.2) ──┤
  └── 1.4 翻前 GTO 表 (依赖 1.3) ─┘
                                    │
阶段二: SelfPlayEnv 完善             │
  └── 2.1 完整规则引擎 (独立) ──────┤
                                    │
                                    ▼
阶段三: DRL 训练 + 推理
  ├── 3.1 环境封装 (依赖 阶段二) ──┐
  ├── 3.2 PPO 训练 (依赖 3.1) ────┤
  └── 3.3 DRL 推理 (依赖 3.2) ────┘
                                    │
                                    ▼
阶段四: 决策融合层 (依赖 阶段一 + 阶段三)
                                    │
                                    ▼
阶段五: 集成测试 + 评估 (依赖 全部)
```

**关键路径**: 阶段一 → 阶段四（CFR 是融合层的硬依赖）

**可并行**:
- 阶段一 和 阶段二 可以**同时进行**（不同开发者）
- 阶段三 的 3.1 环境封装可以与阶段一并行；3.2 PPO 训练可与 CFR 翻前表生成并行

---

## 五、技术决策与设计考量

### 5.1 为什么 C++ 做 CFR 而不是 Python？

| 维度 | C++ | Python |
|---|---|---|
| 单次 traverse 速度 | ~10μs | ~500μs+ |
| 100K 迭代 × 10K 节点 | ~3 分钟 | ~2.5 小时 |
| 内存占用 | 紧凑 struct | GC 开销 |

> **结论**: CFR 遍历涉及百万级递归调用，必须在 C++ 端实现。

### 5.2 为什么 DRL 不做进 C++？

1. PyTorch 生态成熟，Python 端有 `stable-baselines3` 可直接参考
2. DRL 训练是离线过程，不要求极致延迟
3. 只需保证推理延迟在 2ms 内（GPU 推理绰绰有余）
4. 未来可选 TorchScript → C++ libtorch 推理

### 5.3 信息集抽象的权衡

| 方案 | 信息集数量 | 精度 | 实现难度 |
|---|---|---|---|
| 无抽象（精确） | 极大 (~10^160) | 精确 | 不可行 |
| 手工特征桶 (本项目) | ~10^5~10^6 | 中等 | 中等 |
| 深度学习抽象 (Deep CFR) | ~10^3 | 高（但需训练神经网络） | 高 |

> **选择**: 手工特征桶方案。理由：作为初版基线，实现可靠、调试方便、无额外训练成本。

### 5.4 PPO 中的对手选择策略

| 阶段 | 对手 | 目的 |
|---|---|---|
| 初期 (0-1M steps) | Random / TAG / LAG 各 1/3 | 探索多样策略空间 |
| 中期 (1M-3M steps) | 70% 上次 checkpoint + 30% 随机 | 渐进式自我提升 |
| 后期 (3M+ steps) | 最新 checkpoint + 历史最强 + 随机 (4:4:2) | 防止策略退化 |

---

## 六、文件清单 — 新建/修改

### 新建文件

| 文件 | 用途 |
|---|---|
| `规划.md` | 本文档 |
| `scripts/train_preflop.py` | 离线训练翻前 CFR GTO 策略表 |
| `scripts/train_cfr_postflop.py` | 离线训练翻后 CFR 基线策略 |
| `scripts/eval_elo.py` | 两个 AI 版本 Elo 对战脚本 |
| `scripts/run_selfplay.py` | 自博弈数据生成脚本 |
| `drl/features.py` | 特征提取模块（从 GameState→tensor，复用于训练和推理） |
| `drl/eval.py` | DRL 模型评估脚本 |
| `cpp/src/abstraction.cpp` | 信息集抽象实现（从 cfr_engine.cpp 拆分） |
| `cpp/include/poker/abstraction.h` | 信息集抽象头文件 |
| `cpp/tests/test_abstraction.cpp` | 信息集抽象单元测试 |
| `cpp/tests/test_environment.cpp` | 环境规则引擎单元测试 |
| `tests/integration/` | 集成测试目录 |

### 修改文件

| 文件 | 改动 |
|---|---|
| `cpp/src/cfr_engine.cpp` | 🔴 大规模：实现 MCCFR 遍历 + 策略持久化 |
| `cpp/src/environment.cpp` | 🔴 大规模：实现完整德州扑克规则 |
| `cpp/include/poker/cfr_engine.h` | 🟡 扩展：添加 CFRNode 等新结构 |
| `cpp/include/poker/environment.h` | 🟡 扩展：添加阶段推进、下注轮逻辑 |
| `drl/train.py` | 🔴 大规模：实现 PPO 完整训练循环 |
| `drl/infer.py` | 🔴 大规模：替换硬编码为真实推理 |
| `drl/network.py` | 🟡 小改：可能调整网络结构 |
| `engine/fusion.py` | 🟡 中等：动态权重 + 对手偏移 + 下注尺度 |
| `engine/parser.py` | 🟡 小改：补全 ActionRecord 追加逻辑 |
| `cpp/python_bindings/bindings.cpp` | 🟡 扩展：暴露新 C++ 接口到 Python |
| `cpp/CMakeLists.txt` | 🟡 添加新源文件和测试 |

---

## 七、风险与缓解

| 风险 | 概率 | 影响 | 缓解措施 |
|---|---|---|---|
| MCCFR 遍历收敛慢 | 中 | 翻前策略不准 | (1) 抽象桶数可调 (2) 使用 Chance Sampling 加速 (3) 必要时降到 5 桶 |
| 信息集爆炸 | 中 | 内存溢出 | 限制动作历史深度为最近 3 轮；增加抽象力度 |
| PPO 训练不稳定 | 高 | 策略退化 | (1) 多环境并行平滑梯度 (2) 从 CFR 策略蒸馏初始化 (3) 降低 clip_range |
| Python↔C++ 往返延迟高 | 低 | 决策超时 | (1) pybind11 直接传指针 (2) 必要时用 TorchScript 推至 C++ |
| CUDA 显存不足 (4060 笔记本) | 中 | batch_size 受限 | 减小 batch_size (1024→512)；启用梯度累积；使用 mixed precision |
| 网络断连/比赛平台协议变更 | 低 | 比赛中崩 | (1) 超时 fallback 到 call (2) 协议版本号检测 (3) 记录原始消息日志 |

---

## 八、里程碑与时间线

```
Week 1-2:  ██████████ 信息集抽象 + MCCFR 基本遍历
Week 2-3:  ██████████ CFR 策略持久化 + 翻前表生成
            ██████████ SelfPlayEnv 规则引擎 (可并行)
Week 4-5:  ██████████ DRL 环境封装 + 特征工程
Week 5-7:  ██████████ PPO 训练 + DRL 推理
Week 7-8:  ██████████ 决策融合层完善
Week 8-10: ██████████ 集成测试 + Elo 评估 + Bug 修复

总计: ~10 周 (2.5 个月)
```

### 里程碑 M1 (第 3 周末): CFR 可运行
- MCCFR 遍历通过单元测试
- 翻前策略表生成并与公开 GTO 对比

### 里程碑 M2 (第 5 周末): 环境闭环
- SelfPlayEnv 能完整跑一局
- DRL 环境能产生训练数据

### 里程碑 M3 (第 8 周末): 端到端决策
- AI 通过 Mock 服务器进行完整对局
- 融合决策层输出合法动作

### 里程碑 M4 (第 10 周末): 交付
- Elo vs 随机 AI > 500 分
- Elo vs 简单启发式 AI > 200 分
- 无内存泄漏，稳定运行 10000 局

---

## 九、团队分工建议

| 角色 | 负责模块 | 技能要求 |
|---|---|---|
| **C++ 核心开发** (1-2人) | CFR 引擎 / SelfPlayEnv / 性能优化 | C++17, 游戏 AI 算法, CMake |
| **Python ML 开发** (1-2人) | DRL 训练 / 推理 / 特征工程 | PyTorch, RL (PPO), numpy |
| **系统工程** (1人) | 融合层 / 协议 / 集成测试 / 评估 | Python, 系统集成, 测试框架 |

---

## 十、附录

### A. 参考论文
- *Monte Carlo Sampling for Regret Minimization* (Lanctot et al., 2009) — MCCFR 基础
- *DeepStack* (Moravčík et al., 2017) — 连续解决 + 深度学习
- *Libratus* (Brown & Sandholm, 2017) — 子博弈求解
- *Pluribus* (Brown & Sandholm, 2019) — 6 人桌 CFR+

### B. 参考实现
- [PyPokerEngine](https://github.com/ishikota/PyPokerEngine) — Python 扑克环境
- [OpenSpiel](https://github.com/deepmind/open_spiel) — DeepMind 游戏 AI 框架
- [ACPC (Annual Computer Poker Competition)](http://www.computerpokercompetition.org/) — 标准协议

### C. 配置参数速查

| 参数 | 默认值 | 说明 |
|---|---|---|
| `cfr.iterations` | 100,000 | 翻前 CFR 迭代次数 |
| `cfr.hand_buckets` | 10 | 手牌抽象桶数 |
| `drl.learning_rate` | 3e-4 | PPO 学习率 |
| `drl.batch_size` | 2048 | SGD mini-batch 大小 |
| `drl.n_envs` | 16 | 并行环境数 |
| `fusion.alpha` | 0.5 | CFR/DRL 融合权重 |
| `DECISION_TIMEOUT` | 5.0s | 决策超时阈值 |
